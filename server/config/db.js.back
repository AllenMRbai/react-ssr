const env = process.env.NODE_ENV;

let redisConfig;
if (env === "production") {
  redisConfig = {
    host: "localhost",
    port: "6379"
  };
} else if (env === "development") {
  redisConfig = {
    host: "localhost",
    port: "6379"
  };
}

module.exports = {
  redisConfig
};
